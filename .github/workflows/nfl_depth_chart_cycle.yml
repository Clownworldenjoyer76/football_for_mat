name: NFL Depth Chart Pull

on:
  workflow_dispatch:

jobs:
  pull-depth-chart:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run depth_chart.py
        run: |
          python docs/win/football/nfl/scripts/00_intake/depth_chart.py

      - name: Commit output file
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add docs/win/football/nfl/data/raw/raw_depth.csv
          git diff --staged --quiet || git commit -m "Update raw_depth.csv"
          git push
