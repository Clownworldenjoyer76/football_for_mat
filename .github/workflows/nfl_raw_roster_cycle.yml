name: NFL Raw Roster Pull

on:
  workflow_dispatch:

jobs:
  pull-raw-roster:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run pull_raw_roster.py
        run: |
          python docs/win/football/nfl/scripts/00_intake/pull_raw_roster.py

      - name: Commit output file
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add docs/win/football/nfl/data/raw/raw_roster.csv
          git diff --staged --quiet || git commit -m "Update raw roster data"
          git push
