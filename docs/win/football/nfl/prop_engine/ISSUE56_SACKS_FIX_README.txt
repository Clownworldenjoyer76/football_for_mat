ISSUE 56 — SACKS DENOMINATOR FIX

Purpose
-------
Correct only the confirmed dimensional mismatch for the sacks component:
sack_rate_per_defensive_play is multiplied by opponent_offensive_plays,
not opponent_dropbacks.

Files changed by the apply helper
---------------------------------
1. scripts/train/select_model_architecture.py
   - sacks component dependency
   - sacks historical component formula

2. scripts/project/project_week.py
   - sacks current-week component formula

3. validate_issue36.py
   - sacks reconstruction formula

4. evaluate_production_approval.py
   - only if a local old sacks formula is detected there; otherwise untouched

Generated files are NOT hand-edited.
models/sacks/selected_model.json is regenerated by model selection.
Current-week feature manifests/logs remain generated artifacts and will refresh
when the normal current-feature/production path is run.

Rerun sequence
--------------
Run run_issue56_sacks_fix.ps1 from PowerShell.

It performs:
- targeted source patch
- source validation
- existing calibration-point-fix validation, if present
- model architecture rescoring/reselection
- post-selection metadata validation
- uncertainty recalibration
- production approval evaluation

No opportunity or efficiency model retraining is performed because their
definitions/models are unchanged. Architecture selection re-scores the existing
components using the corrected dimensional formula.

Methodology
-----------
select_model_architecture.py keeps its existing chronological policy:
architecture/blend selection on 2024 validation, 2025 reporting only.
calibrate_uncertainty.py keeps its existing OOF validation-only calibration.
No approval thresholds are changed.

Do not hand-edit production_registry or generated selected-model metadata.
